# Alpha Manual Run Log

Use this file when running the manual alpha path. Keep entries short and actionable.

## Run Metadata

- Date: 2026-04-25
- Build / commit: pushed `main` on 2026-04-25 after the runtime schema smoke coverage updates
- Tester: Codex
- Local verification run: targeted main/preload tests, `npm run lint`, `npm run build`, `npm run verify`, `npm run accept:agent-local`, `npm run accept:provider-native-live:preflight`
- Smoke check run: `npm run dev` manual launch, isolated `TASKPLANE_USER_DATA_DIR=/tmp/taskplane-alpha-20260424-fresh npm run dev`, `npm run smoke:build`, `npm run smoke:package:mac`, `npm run smoke:runtime:mac`, `npm run smoke:release:mac`, `npm run release:mac:preflight`

## Result Summary

- Overall result: `pass with issues`
- Highest severity issue: dev launch path was blocked until Electron startup, native-module ABI, dev URL, and preload format issues were fixed.
- Follow-up owner: project maintainers

## Findings

| Area | Step | Result | Finding | Severity | Follow-up |
| --- | --- | --- | --- | --- | --- |
| Verification Gate | `npm run verify` | Pass | Initially failed while `better-sqlite3` was rebuilt for Electron ABI; passed after adding and running `npm run rebuild:node`. | Medium | Keep Node/Electron ABI switch documented. |
| Verification Gate | `npm run smoke:build` when needed | Pass | Updated smoke check for `bootstrap.cjs` and `preload.cjs`; build smoke passes. | Low | Keep smoke check aligned with entrypoint changes. |
| Dev Launch | Start `npm run dev` | Pass | App launches to Home after clearing `ELECTRON_RUN_AS_NODE`, rebuilding native modules for Electron, loading `localhost`, using a CJS preload bundle, and prebuilding fresh Electron outputs before watchers start. | High | Keep as regression guard in development docs. |
| Dev Launch | Isolated userData env | Pass with issue | First isolated attempt used stale compiled output and wrote `Alpha manual task` to the default DB; after adding the prebuild step, Settings showed `/tmp/taskplane-alpha-20260424-fresh/config.json`. Accidental default-DB test data was deleted after approval. | Medium | Keep isolated userData required for manual alpha runs. |
| Core Task Loop | Create task and open detail | Pass | Created `Alpha manual task` in isolated userData; task event appeared and detail pane opened. | Low | Continue richer task-object coverage. |
| Core Task Loop | Update summary / next step / state | Pass | Saved summary and next step; list card and Home recovery preview reflected the next-step update. Later, in packaged app with isolated `TASKPLANE_USER_DATA_DIR=/tmp/taskplane-alpha-state-transition-20260424`, created `State transition alpha check` and used the task UI status controls to transition it from `captured` to `planned`; SQLite confirmed the final task state and `task.transitioned` timeline payload. A later renderer fix added compact task-detail section jumps for Current, Completion, Action, Activity, and Context Studio; packaged app with `/tmp/taskplane-alpha-jumpnav-20260424` confirmed the jump bar appears and `动作` jumps from Context Studio back to Action Desk. | Low | Keep further detail-navigation changes scoped to small acceptance fixes. |
| Context Objects | Add source/process/blocker/dependency | Pass with issue | In isolated userData, created `Prepare alpha onboarding brief`, saved summary/next step through UI, then seeded one key source, one bound process checklist, one active blocker, and two completion criteria to continue recovery validation. Later, in packaged app with isolated `TASKPLANE_USER_DATA_DIR=/tmp/taskplane-alpha-dependency-ui-20260424`, created upstream/downstream tasks and added an active dependency from downstream to upstream through the task UI. After adding first-screen context shortcuts, packaged app with `/tmp/taskplane-alpha-context-shortcut-20260424` showed `新增来源材料` / `新增方法模板`; `新增来源材料` jumped to Source Context instead of Action Setup. In a clean packaged-app run with `/tmp/taskplane-alpha-db-probe-20260424`, created `DB source` as a key link source and `DB process` as an active bound method from the task UI; SQLite confirmed one active `source_contexts` row, one active `process_templates` row, and one active `task_process_bindings` row. Finally, packaged app with `/tmp/taskplane-alpha-source-process-edit-20260424` edited a seeded source note to `Edited source via UI`, archived that source, archived a bound process template, and saved a process summary edit to `Edited process via UI`; SQLite confirmed the source archived state and process edit/archive checkpoints. | Low | Context Studio access is now helped by the packaged-checked section jump bar. |
| Context Objects | Resolve blocker/dependency | Pass with issue | Resolved the active blocker from Home. Home blocked count changed from 1 to 0 and Recent Activity showed the blocker resolution. Later, resolved the downstream task dependency from the task UI; list priority returned from dependency recovery and SQLite showed `task_dependency.created` then `task_dependency.resolved` with final dependency status `resolved`. | Low | Keep any remaining dependency-management friction scoped to small navigation or wording fixes. |
| Decision And Run Loop | Create or draft decision | Pass | Created `Approve alpha acceptance path wording` from the Decisions page against `Validate alpha decision and run loop`; it appeared as pending in the current focus, queue, Home recommended actions, and Pending Decisions. Later, renderer coverage confirmed formal approve/defer/cancel actions are only exposed while a Decision is pending, and resolved Decisions keep only the task recovery path. | Low | Keep future Decision work focused on wording and recovery clarity. |
| Decision And Run Loop | Trigger run and inspect result | Pass with issue | Triggered a draft run without AI credentials. Runs showed `draft / failed`, output and failure reason both explained `AI API Key is not configured in system Keychain`, and the task was annotated high risk with a retry-oriented next step. Home surfaced the failed run and high-risk recovery. | Low | Keep no-key failure wording as the expected local path; configure real keys for successful run validation later. |
| Decision And Run Loop | Trigger real AI-backed run with Replicate | Pass | In dev app with isolated `TASKPLANE_USER_DATA_DIR=/tmp/taskplane-alpha-replicate-run-20260424` and deliberate local `.env` Replicate credentials, created `Replicate alpha successful run check` and triggered a `draft` Run. SQLite confirmed `run_3c335138-e0df-4b18-9e19-16fa6990a827` moved from created to `completed` in about 13s, `output_source=ai`, output length `1540`, `run.completed`, `artifact.created`, and `task.run_completed` timeline events, a `draft output` artifact, and a next-step update. | Low | Keep provider-specific success checks local while GitHub Actions quota is unavailable. |
| Decision And Run Loop | Trigger packaged read-only workspace agent run | Pass | In the packaged app with isolated `TASKPLANE_USER_DATA_DIR=/tmp/taskplane-alpha-workspace-agent-manual-20260425`, `TASKPLANE_WORKSPACE_ROOT=/tmp/taskplane-alpha-workspace-root-manual-20260425`, and deliberate local `.env` Replicate credentials, created `Packaged workspace agent manual 20260425`, enabled `允许只读工作区上下文`, and triggered an `agent` Run. SQLite confirmed `run_46918968-2c5b-4795-9afa-f5bbaa915ee1` completed with `output_source=ai`, agent session `fileContext=true`, `workspace.search` found `docs/marker.txt: manual packaged workspace marker 20260425`, `workspace.read_file` read the marker file, note and run-output artifacts were created, and no checkpoints were opened. | Low | Treat the packaged read-only workspace agent path as alpha-covered; keep write/command tools gated behind the separate decision doc. |
| Decision And Run Loop | Exercise workspace patch approval path | Pass at code level | Added a confirmation-gated `workspace.write_patch` path after the packaged read-only pass. Local verification covers policy denial without `allowLocalFileWrite`, checkpoint creation with expected files and diff preview, no pre-approval mutation, Decision approval resuming the patch through real SQLite repositories, resolved checkpoint state, Runs-page patch preview wording, normal-run fallback when a model proposes patch steps, and rejection for path escapes / files outside `expectedFiles`. `workspace.run_command` now has a separate registry-level `test` / `lint` package-script checkpoint path, but remains absent from normal prompts and task UI. On 2026-04-25, `npm run accept:agent-local` passed after the Runs / Decisions checkpoint summary clarity update. | Medium | Do not expose workspace write or command access in normal prompts until their UI opt-in and checkpoint review UX are deliberately accepted. |
| Agent Execution Layer | Task tool opt-in | Pass at code level | Added service-routed registry tools for `task.update_next_step`, `task.create_completion_criterion`, `source_context.create`, and draft-only `decision.draft`, then exposed them only behind explicit per-run `allowTaskMutationTools` opt-in. A later slice added read-only `task.review_completion_evidence`, which reviews completion status and recent evidence without satisfying criteria or completing the task. Local verification covers prompt gating, run-loop policy gating, Tasks/Runs form capability previews, run-step observations, validation failures, real SQLite timeline evidence, persisted RunService integration for an opted-in task mutation run, and a RunService completion-evidence review path that leaves criteria open and task state unchanged. On 2026-04-25, `npm run accept:agent-local` passed with the focused domain-tool slice included. | Medium | Keep workspace write/command tools separate from task update/evidence opt-in; keep stronger closeout mutations behind a separate accepted decision. |
| Agent Execution Layer | Provider-native live readiness | Pass without live call | On 2026-04-25, `npm run accept:provider-native-live:preflight` reported the local `.env` provider-native setup as ready with configured `fal-openrouter`, `google/gemini-2.5-flash`, a set API key, and provider-native tool calls enabled. No live provider request was run during this step. | Low | Run live provider-native validation only when deliberately spending test credit is acceptable. |
| Code Agent UI / Sandbox Producer | Manual local non-live validation | Pass | On 2026-04-26, the first visible code-agent UI slices were validated without GitHub Actions and without external AI calls. `npm run accept:sandbox-coding:backend-preflight` reported local-container backend ready with Docker Desktop available and `dockerServer=29.3.1`. Default producer preview smoke passed in skipped mode with Docker not started and AI not called. Explicit non-live preview smoke passed with workspace unchanged, Docker not started, and AI not called. Explicit Docker-check preview smoke passed with Docker checks started, workspace unchanged, and AI not called. `npm run accept:sandbox-coding` passed with 26 files / 165 tests, and `npm run verify` passed afterward with 90 files / 641 tests plus lint/build. | Low | This validated readiness, intent, lifecycle review, and automatic-start policy before the dedicated manual preview run wiring was added. |
| Code Agent UI / Sandbox Producer | Manual sandbox preview run wiring | Pass at code level | On 2026-04-26, the Task detail Code Agent button was wired to a dedicated `run:triggerCodeAgent` IPC path. The path creates a real `agent` Run, requires operator confirmation, passes only selected `test` / `lint` checks, invokes the local-container producer execution service, and opens Runs detail for lifecycle/source evidence review. Source-ready previews now persist a patch artifact, open `patch_promotion` checkpoint, and pending Decision through the existing patch-review persister. The first producer loop writes only a staged `.taskplane/code-agent-preview.md` diagnostic and clearly labels the real model producer loop as not connected. Internal `session.json` sandbox manifests are now excluded from staged patch collection. Focused renderer/main tests, `npm run lint`, `npm run build`, and `npm run accept:sandbox-coding` passed locally. | Medium | Next execution work should connect a real model-backed producer loop; keep generic tools, automatic starts, and external providers out of this preview path until separately accepted. |
| Code Agent UI / Sandbox Producer | Preflight summary and check availability gate | Pass at code level | On 2026-04-26, the Task detail Code Agent panel gained a compact preflight summary for runtime readiness, selected `test` / `lint` checks, local/model producer mode, context-file requirement, Decision promotion, and next required action. AI config status now reads only `workspaceRoot/package.json` to detect available `test` / `lint` scripts; unavailable scripts render disabled in the UI and are rechecked in `run:triggerCodeAgent` before a Run is created. `npm run accept:sandbox-coding:code-agent-ui`, `npm run accept:sandbox-coding`, `npm run lint`, `npm run build`, `npm run smoke:build`, and `npm run smoke:release:mac` passed locally without GitHub Actions; the release smoke used unsigned/ad-hoc macOS packaging and an isolated runtime data directory. | Low | Packaged/manual validation should next confirm the preflight summary is understandable in the real Task detail flow before broadening model-backed Code Agent usage. |
| Code Agent UI / Sandbox Producer | Patch promotion apply smoke | Pass | On 2026-04-26, `npm run accept:sandbox-coding:patch-promotion-apply-smoke` passed on a disposable workspace with isolated user data. Default mode resolved the Decision with `default=no-write`; apply-enabled mode wrote only the reviewed temp-workspace file and marked the durable promotion record `applied`; Docker was not started and AI was not called. | Medium | Keep apply behavior behind `TASKPLANE_ENABLE_SANDBOX_PATCH_PROMOTION_APPLY` until a broader manual alpha pass accepts the UX and failure recovery path. |
| Code Agent UI / Sandbox Producer | Model-backed preview smoke | Pass | On 2026-04-26, the env-gated `TASKPLANE_RUN_CODE_AGENT_MODEL_PRODUCER_PREVIEW_SMOKE=true npm run accept:sandbox-coding:model-producer-preview-smoke` path passed with `fal-openrouter` / `google/gemini-2.5-flash`, selected bounded context, one provider call, no Docker, and no selected-workspace mutation. The first attempt revealed provider fenced-JSON wrapping; the staged-file parser now accepts one fenced JSON object before applying the same path/content/size contract. A later rerun with shell-only env overrides, `TASKPLANE_CODE_AGENT_CONTEXT_FILES=package.json`, and the same provider/model again reached `preview_ready`, staged `.taskplane/code-agent-model-producer-preview-smoke.md`, did not start Docker, and left the selected workspace unchanged. | Medium | Keep this path explicit and provider-spend-aware before exposing model-backed Code Agent as a normal UI switch. |
| Code Agent UI / Sandbox Producer | Model producer preflight ready check | Pass | On 2026-04-26, the default local `.env` state correctly reported `status=skip` because sandbox/model-producer flags and workspace root were not enabled. A shell-only override with `TASKPLANE_ENABLE_CODE_AGENT_MODEL_PRODUCER=true`, `TASKPLANE_ENABLE_SANDBOX_CODING_AGENT=true`, `TASKPLANE_WORKSPACE_ROOT=/Users/caoq/git/Taskplane`, and `TASKPLANE_CODE_AGENT_CONTEXT_FILES=package.json` reported `status=ready` for `fal-openrouter` / `google/gemini-2.5-flash` with one selected context file and no provider request, Docker probe, or workspace mutation. | Low | Keep ready checks as preflight-only; run provider-spending preview smoke only when deliberately validating the model path. |
| Code Agent UI / Sandbox Producer | Task detail UI manual pass | Pass with issue fixed | On 2026-04-26, the dev app was launched with isolated `TASKPLANE_USER_DATA_DIR` and disposable `TASKPLANE_WORKSPACE_ROOT` from `npm run manual:code-agent-ui-fixture`, scheduler disabled, sandbox coding enabled, model producer enabled, and patch-promotion apply disabled. Created `Code Agent UI manual alpha check`, opened Action Desk, and checked runtime readiness. The first UI pass exposed long runtime/check/preflight text and workspace paths squeezing the Code Agent card; the renderer now lets Task detail panels shrink correctly, wraps long meta text, and makes Action Setup plus the Run/Code Agent card span the detail grid. The final UI pass showed `ExecutionRuntime：ready`, provider spend still opt-in, preflight blocked on explicit Docker/Decision review, and no provider call. | Low | Keep the real Task detail UI pass as the next visual guard before broadening model-backed Code Agent usage. |
| Code Agent UI / Sandbox Producer | Task detail staged patch Decision pass | Pass with issue fixed | On 2026-04-26, reused the isolated manual fixture and started a Task detail Code Agent sandbox preview with `Use model producer` unchecked and `lint` selected. The run completed with `checks=lint: passed`, recorded readable Docker check evidence, produced a staged `.taskplane/code-agent-preview.md` patch artifact, opened a `patch_promotion` checkpoint, and created a pending promotion Decision. Runs detail showed `Staged Patch Review`, `Promotion readiness: ready`, and `打开 promotion Decision`; clicking it opened the matching `workspace.staged_patch` Decision. Earlier UI attempts showed false `lint` / `test` failures until the local-container command runner inherited the host process environment for Docker CLI execution. | Medium | Keep approval/apply disabled unless explicitly validating the patch-promotion apply path; the accepted path is review-first and workspace unchanged before Decision approval. |
| Code Agent UI / Sandbox Producer | Task detail model-backed staged patch pass | Pass with issue fixed | On 2026-04-26, launched the dev app with isolated `TASKPLANE_USER_DATA_DIR`, disposable `TASKPLANE_WORKSPACE_ROOT`, `TASKPLANE_ENABLE_SANDBOX_CODING_AGENT=true`, `TASKPLANE_ENABLE_CODE_AGENT_MODEL_PRODUCER=true`, `TASKPLANE_CODE_AGENT_CONTEXT_FILES=docs/code-agent-context.md`, and patch-promotion apply disabled. The preflight was ready with `fal-openrouter` / `google/gemini-2.5-flash`, one 406-byte context file, and no provider call until the UI run was started. From Task detail, selected `Use model producer`, provided explicit context, confirmed Docker/Decision review, and spent one provider request. The run completed as `source_ready`, recorded context collection, staged `.taskplane/model-backed-ui-acceptance.md`, passed both `lint` and `test`, created a pending promotion Decision, and kept the workspace unchanged before approval. The pass also exposed that Task detail recovery should not depend on lightweight run-list output text; the Code Agent Review card now falls back to the pending `workspace.staged_patch` Decision plus latest agent run. | Medium | The real model-backed UI path is alpha-validated for staged review. Next work should improve evidence summaries or approval/apply UX only behind the existing Decision boundary. |
| Code Agent UI / Sandbox Producer | Staged patch lifecycle recovery UX | Pass at code level | Runs detail now expands the staged patch review into a persisted evidence checklist for source evidence, targeted checks, promotion Decision, and workspace mutation state, plus a `Next review move` derived from failed checks, missing/open Decisions, applied patches, or deferred/no-write workspace state. Runs can route back to the Task Code Agent input surface with a bounded rerun intent, and Task detail recovery is anchored on pending `workspace.staged_patch` Decisions with checkpoint-to-Run lookup and a separate rerun-prep affordance. | Low | This is a no-provider, no-Docker UI/recovery slice; use the next isolated manual UI pass to confirm the checklist and rerun affordances read cleanly with real model-backed evidence. |
| Browser Evidence | Operator-started Runs UI smoke | Pass | On 2026-04-27, launched the dev app with isolated `TASKPLANE_USER_DATA_DIR=/tmp/taskplane-browser-evidence-ui-20260427`, scheduler disabled, and no provider call. The first start required `npm run rebuild:electron` because `better-sqlite3` had been rebuilt for Node; after that the app launched cleanly. Created `Browser Evidence UI smoke 20260427`, opened Runs / Action Desk, clicked `运行 Browser Evidence Smoke`, and the run completed as `agent / completed`. Runs detail showed the Browser Evidence card with page summary / visible text / screenshot artifact kinds, screenshot path, accepted/captured/artifact RunSteps, and related task timeline. SQLite confirmed one completed agent run, one `browser_evidence` artifact, and `run.created` / `artifact.created` / `run.completed` timeline events in the isolated DB. | Low | Tier 1 browser evidence is locally accepted through the manual UI path; pause broad browser work and return to orchestration unless the next review finds UX polish issues. |
| Completion Loop | Add/satisfy/reopen criteria | Pass | In packaged app with isolated `TASKPLANE_USER_DATA_DIR=/tmp/taskplane-alpha-criteria-ui-20260424`, created `Persistence smoke task`, added `Packaged criteria path is persisted and can be reopened after satisfaction.` through the task UI, marked it satisfied, reopened it, and confirmed the final open criterion persisted in SQLite. A later renderer fix added direct section jumps to Completion and Action. In the current local packaged test DB, created `Completion UI finish check`, added `Completion UI flow is verified from task detail.`, marked it satisfied, and confirmed SQLite kept the criterion as `satisfied`. | Low | Keep criteria edits scoped; the pure UI create/satisfy path is now covered. |
| Completion Loop | Inspect closeout evidence | Pass | Task criteria display showed the failed run as potential evidence but correctly did not mark the task closeout-ready while an open criterion and high-risk run failure remained. Later, seeded a packaged-app isolated fixture under `TASKPLANE_USER_DATA_DIR=/tmp/taskplane-alpha-closeout-ui-20260424` with one planned task, one satisfied criterion, and one approved decision; Home showed the closeout headline, recommended action, `可收尾` status, final decision evidence, and opened the Decision from `查看最终收尾依据`. The pure UI packaged pass for `Completion UI finish check` then surfaced closeout-ready Home wording, prioritized `转到 completed（完成标准已满足）` first in Action Desk after a renderer fix, and SQLite confirmed `captured -> planned -> running -> completed` timeline events. | Low | Front task-management closeout is covered enough to move next focus to agent execution design. |
| Home Recovery Loop | Open items from Home | Pass | Home opened the created task from resume preview and exposed key source, current method, active blocker, and blocker source actions. After the unique-task headline fix, a packaged app fixture under `TASKPLANE_USER_DATA_DIR=/tmp/taskplane-alpha-home-open-items-20260424` showed one blocked task in the Home headline, surfaced key source/recent activity/key signal/resume preview entries, and routed those entries back to the expected task detail, source context, and blocker recovery targets. A later renderer pass added a dedicated `重新评估决策` action for cancelled Decision recent activity, prefilled with an alternative-path next step instead of falling through to a generic task open. | Low | Re-check Home-to-detail recovery together with the new section jump bar. |
| Home Recovery Loop | Inspect priority ordering | Pass with issue | With one source-linked active blocker, Home headline incorrectly said `2 条任务` because blocker follow-up and source re-evaluation were both counted as tasks. Resume preview also showed `查看 Run` for a blocker-resolved activity. Later, a run failure re-sorted a captured task as a fresh task activity because captured-task activity used task `updatedAt`. After the fixes, Home showed failed run activity above the original captured event, surfaced closeout progress as `1 / 2`, and then prioritized a closeout-ready task with approved-decision evidence. A later Replicate-backed successful draft run provided real completed-run timeline/artifact evidence. | Medium | Keep remaining Home follow-ups focused on navigation and wording friction. |
| Settings And Local Config | Save settings and inspect status | Pass | Settings saved non-sensitive config with an empty API key into `/tmp/taskplane-alpha-decision-20260424/config.json`; UI emitted `settings.changed`, retained `API Key 尚未存入系统 Keychain`, and left scheduler disabled. Later, the dev app loaded deliberate local `.env` Replicate settings and Home showed the configured provider/model before the successful AI-backed run. | Low | Keep secret-bearing validation in ignored local env files or OS keychain only. |
| Release Readiness | Build/smoke/package readiness | Pass with issues fixed | `npm run build`, `npm run smoke:build`, `npm run dist:mac:dir`, `npm run smoke:package:mac`, `npm run smoke:runtime:mac`, and `npm run smoke:release:mac` pass locally. The unpacked app exists at `release/mac-arm64/Taskplane.app` (301M), includes `app.asar`, unpacked native modules for `better-sqlite3` and `keytar`, valid `Info.plist` metadata, and passes local code-sign verification. Package smoke now checks bundle structure, required ASAR entries, native module unpacking, executable bit, code signature, and absence of compiled app test files. Runtime smoke launches the packaged executable with isolated `TASKPLANE_USER_DATA_DIR`, clears shell-level `ELECTRON_RUN_AS_NODE`, and confirms `config.json` plus `taskplane.db` are created with the core SQLite schema initialized. Earlier packaged runtime blockers were fixed: isolated user data, production renderer asset paths under `file://`, native-module ABI rebuilds, packaged Electron entrypoint startup, and stale compiled test files in `dist-electron`. On 2026-04-25, `npm run release:mac:preflight` reported `notarytool` and package metadata available, but signed/notarized release remains not ready because Developer ID and Apple notarization credentials are not configured. | Medium | Keep actual signed/notarized release execution deferred; use `npm run smoke:release:mac` as the local unsigned macOS release gate and `npm run release:mac:preflight` as the read-only prerequisite check until signing is configured. |

## Notes

- Record copy/layout friction separately from functional bugs.
- Do not expand the domain model during the run; capture new ideas as follow-up candidates.
- Do not manually dispatch or watch GitHub Actions while Actions quota is unavailable.
